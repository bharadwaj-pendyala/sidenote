{
  "name": "@sidenote/daemon",
  "version": "0.0.1",
  "private": true,
  "description": "Local sidenote daemon: comment store and agent resolve loop. Bundled into the sidenote-cli package; not published separately.",
  "type": "module",
  "bin": {
    "sidenote-daemon": "server.js"
  },
  "scripts": {
    "start": "node server.js",
    "verify": "node verify.js"
  },
  "license": "MIT"
}
